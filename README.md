# Edge-WebView2-Warmup
Edge WebView2 Launcher is a small PowerShell utility that finds and launches the newest Microsoft Edge WebView2 runtime. Used as a warm‑up tool in Windows Startup or Task Scheduler, it initializes WebView2 early and improves the initial responsiveness of Edge and WebView2 apps.
